# BadCodec SPEC
**rev.11 / 2026-03-10**  
**This document is the complete specification. Implementation is possible from this document alone.**

---

## 1. 概要及び共通仕様

**対象：** 低スペックマイコン向けモノクロ動画（白黒2値）  
**用途：** 動画圧縮  
**ブロックサイズ：** 8×8（64ドット）固定  
**エンディアン：** リトルエンディアン（全数値共通）

### 共通ルール

1. 走査線の起点は左上 (x0,y0) とする
2. 色は存在せず、便宜上 0=黒 / 1=白 と表記する
3. 全数値はリトルエンディアンで格納する
4. 未定義部分は「不明／実装依存」と明示する

> ⚠️ **BMP形式はビッグエンディアンのため、エンコーダでBMP読み込み時に必ずバイト順変換を行うこと**

---

## 2. ファイル構造

BadCodecファイルは以下の順序で構成される：

```
┌─────────────────────────────────┐
│ ファイルヘッダー                 │
│ （固定長：20バイト）             │
├─────────────────────────────────┤
│ フレームデータ                   │
│ （可変長：フレーム数分繰り返し） │
│  FRAME_DELIMITER                │
│  フレーム命令 or ブロック命令列  │
│  FRAME_DELIMITER                │
│  フレーム命令 or ブロック命令列  │
│  ...                            │
└─────────────────────────────────┘
```

---

## 3. ファイルヘッダー仕様

### 3-1. ヘッダー構造

ヘッダーは **ヘッダーサイズ（2バイト）+ Fletcher-16チェックサム（2バイト）+ ヘッダー本体** で構成される。

```
オフセット  サイズ  内容
0x00        2      ヘッダー全体サイズ（リトルエンディアン uint16）
0x02        2      ヘッダー本体のFletcher-16チェックサム（リトルエンディアン uint16）
0x04        N      ヘッダー本体
```

### 3-2. ヘッダー本体

```
オフセット  サイズ  型        内容
0x00        3      char[3]   マジックナンバー "Bad"（ASCIIリテラル）
0x03        2      uint16    コーデックバージョン（現行：510）
0x05        2      uint16    カラー数（固定値：2、将来拡張用）
0x07        2      uint16    画像幅（ピクセル）
0x09        2      uint16    画像高さ（ピクセル）
0x0B        2      uint16    ブロックサイズ（固定値：8）
0x0D        2      uint16    総フレーム数
```

**ヘッダー本体サイズ：** 3 + 2×6 = **15バイト固定**  
**ヘッダー全体サイズ：** 2（サイズフィールド）+ 2（Fletcher-16）+ 15（本体）= **19バイト固定**

### 3-3. Fletcher-16整合性チェック

**採用理由：** MD5に対してコード量1/20・RAM使用量1/32・8ビット演算のみで完結。マイコンデコーダに最適。

**算出アルゴリズム：**

```
s1 = 0
s2 = 0
for each byte b in header_body:
    s1 = (s1 + b) mod 256
    s2 = (s2 + s1) mod 256
result = (s2 << 8) | s1   ← リトルエンディアン2バイトで格納
```

**C言語実装例（マイコン向け）：**

```c
uint16_t fletcher16(const uint8_t *data, uint8_t len) {
    uint8_t s1 = 0, s2 = 0;
    while (len--) {
        s1 += *data++;
        s2 += s1;
    }
    return ((uint16_t)s2 << 8) | s1;
}
```

```
コード量 : 約20バイト（LGT8F328 Flashに対して無視できる量）
RAM      : 2バイト（s1, s2のみ）
演算     : 8ビット加算のみ・乗除算なし・テーブル不要
```

**検証手順：**

```
1. オフセット0x02から2バイトを読み取りchk_storedとする
2. オフセット0x04からヘッダー本体（15バイト）を読み取る
3. ヘッダー本体のFletcher-16を算出しchk_calcとする
4. chk_stored == chk_calc であれば継続
5. 不一致の場合は "Fletcher-16 Mismatch!" としてデコード中断
```

**検出能力：**

```
単純チェックサムとの差異：
  単純チェックサム : バイト順の入れ替えを検出できない
  Fletcher-16     : s2がs1の累積和のためバイト順入れ替えも検出可能

用途に対する十分性：
  BadCodecヘッダーは15バイト固定
  偶発的な破損検出が目的
  暗号学的安全性は不要
  → Fletcher-16で必要十分
```

---

## 4. フレームデータ構造

### 4-1. フレームの区切り

各フレームは **FRAME_DELIMITER（0x38）** で区切られる。

```
[FRAME_DELIMITER] [フレーム命令またはブロック命令列] [FRAME_DELIMITER] ...
```

FRAME_DELIMITERはフレームの開始と終了の両方を兼ねる（CSVのカンマと同じ作用）。

### 4-2. フレーム先頭オペコード一覧

各フレームの先頭1バイトがフレーム全体の表現方法を決定する。

| オペコード値 | 命令名 | 説明 |
|------------|--------|------|
| `0x30`〜`0x37` | RLE_FRAME | フレーム全体RLE（走査パターン8種）★rev.13 |
| `0x38` | FRAME_DELIMITER | フレーム区切り |
| `0x39` | SKIP_FRAME | 前フレームをそのまま維持 |
| `0x3A` | FRAME_FILL_BLACK | フレーム全体を黒で塗りつぶす |
| `0x3B` | FRAME_FILL_WHITE | フレーム全体を白で塗りつぶす |
| `0x3C` | BLOCK_STREAM | ブロック命令列の開始（後続にブロック命令が続く）|
| `0x3D` | （空き） | 将来予約 |
| `0x3E` | MASTER_FRAME | フレーム全体rawデータ（後続データあり）|
| `0x3F` | INVERT_PREV_FRAME | 前フレーム全ビット反転 |
| `0xFF` | EXT_PREFIX | 拡張命令プレフィックス |

**RLE_FRAME オペコードのビット構造（0x30〜0x37）：**

```
0 0 1 1 0 [scan_dir] [start_y] [start_x]
            bit2       bit1      bit0

scan_dir : 0=横優先（y外ループ, x内ループ）
           1=縦優先（x外ループ, y内ループ）
start_y  : 0=y=0          1=y=height_max（ヘッダーの height-1）
start_x  : 0=x=0          1=x=width_max （ヘッダーの width-1）
```

| オペコード | scan_dir | start_y | start_x | 説明 |
|-----------|---------|---------|---------|------|
| `0x30` | 横 | y=0 | x=0 | 左上→右→下（デフォルト）|
| `0x31` | 横 | y=0 | x=max | 右上→左→下 |
| `0x32` | 横 | y=max | x=0 | 左下→右→上 |
| `0x33` | 横 | y=max | x=max | 右下→左→上 |
| `0x34` | 縦 | y=0 | x=0 | 左上→下→右 |
| `0x35` | 縦 | y=0 | x=max | 右上→下→左 |
| `0x36` | 縦 | y=max | x=0 | 左下→上→右 |
| `0x37` | 縦 | y=max | x=max | 右下→上→左 |

> `0x30`（横/左上）は旧 RLE_FRAME（rev.11以前）と完全に同一の動作である。

### 4-3. フレームデータの読み取りフロー

```
[FRAME_DELIMITER(0x38)を読む]
       │
       ▼
[次の1バイトを読む]
   ├─ 0x30-0x37 : RLE_FRAME      → 走査パターンを抽出し後続RLEデータを読む
   ├─ 0x38      : FRAME_DELIMITER → SkipFrame（前フレーム維持）
   ├─ 0x39      : SKIP_FRAME      → 前フレームをコピー
   ├─ 0x3A      : FRAME_FILL_BLACK → 全画素を0（黒）で塗りつぶす
   ├─ 0x3B      : FRAME_FILL_WHITE → 全画素を1（白）で塗りつぶす
   ├─ 0x3C      : BLOCK_STREAM    → 後続ブロック命令列を読む
   ├─ 0x3E      : MASTER_FRAME    → 後続rawデータを読む
   └─ 0x3F      : INVERT_PREV_FRAME → 前フレームを全反転
```

---

## 5. 命令体系 確定分木

ブロック命令（BLOCK_STREAM内で使用）の1バイトオペコード体系。

```
Root (1 byte opcode)
│
├─ bit7 = 1 : 制御系
│   ├─ bit6 = 0 : SKIP_BLOCK
│   │     └─ bit5–0 : ブロック数 - 1（実行回数 = 1–64）
│   │
│   └─ bit6 = 1 : FOR
│         └─ bit5–0 : 繰り返し回数 - 2（実行回数 = 2–65）
│               FORのネスト禁止
│
└─ bit7 = 0 : 描画系
    │
    ├─ bit6 = 1 : SHIFT_BIT
    │     bit5   : 符号X（0=+, 1=-）
    │     bit4-3 : X移動量（0-3ドット）
    │     bit2   : 符号Y（0=+, 1=-）
    │     bit1-0 : Y移動量（0-3ドット）
    │
    └─ bit6 = 0
        ├─ bit5 = 0 : BLOCK_INVERT
        │     └─ bit4-0 : 反転ブロック数 - 1（実行回数 = 1–32）
        │
        └─ bit5 = 1
            ├─ bit4 = 0 : RLE_BLOCK_4  (0x20-0x2F)
            │     後続3バイト: 6bit×4ラン
            │
            └─ bit4 = 1
                ├─ bit3 = 0 : FILL_BLOCK  (0x30-0x37)
                │     bit2   : 色（0=黒, 1=白）
                │     bit1-0 : 繰り返し回数（1-4回）
                │
                └─ bit3 = 1 : FRAME_CONTROL  (0x38-0x3F)
                      コンテキスト依存命令群
```

**FRAME_CONTROL 空間 (0x38-0x3F) のブロックコンテキスト割り当て：**

```
0x38 : RLE_BLOCK_8  H/TL 黒基準  後続6バイト（6bit×8ラン）
0x39 : RLE_BLOCK_8  H/TL 白基準  後続6バイト
0x3A : RLE_BLOCK_8  H/TR 黒基準  後続6バイト
0x3B : RLE_BLOCK_8  H/TR 白基準  後続6バイト
0x3C : MASTER_BLOCK              後続8バイト（raw）
0x3D : RLE_BLOCK_8  V/TL 黒基準  後続6バイト
0x3E : RLE_BLOCK_8  V/TL 白基準  後続6バイト
0x3F : XOR_BLOCK                 後続可変長（length + RLE）
```

### オペコード早見表

| 命令 | ビットパターン | 値の範囲 | 後続バイト数 |
|------|-------------|---------|------------|
| SKIP_BLOCK | `10nnnnnn` | 0x80-0xBF | 0 |
| FOR | `11nnnnnn` | 0xC0-0xFF | 0（次命令を繰り返す）|
| SHIFT_BIT | `01sxxyy` | 0x40-0x7F | 0 |
| BLOCK_INVERT | `000nnnnn` | 0x00-0x1F | 0 |
| RLE_BLOCK_4 | `0010pppc` | 0x20-0x2F | 3 |
| FILL_BLOCK | `00110cnn` | 0x30-0x37 | 0 |
| RLE_BLOCK_8 | `00111xxx` | 0x38-0x3B, 0x3D-0x3E | 6 |
| MASTER_BLOCK | `00111100` | 0x3C | 8 |
| XOR_BLOCK | `00111111` | 0x3F | 1+N（可変）|

---

## 6. ブロック命令詳細

### 6-1. SKIP_BLOCK

```
bit7=1, bit6=0
bit5-0 : 値0〜63 → +1オフセット → 実行回数 1〜64

オペコード例：
  0x80 = SKIP 1ブロック
  0xBF = SKIP 64ブロック
```

- 前フレームの同位置ブロックをコピーしカレントブロックを進める
- **SKIP_BLOCK を SHIFT_BIT(X=0,Y=0) より優先すること**（エンコーダルール）

---

### 6-2. FOR

```
bit7=1, bit6=1
bit5-0 : 値0〜63 → +2オフセット → 実行回数 2〜65

オペコード例：
  0xC0 = 次の命令を2回実行
  0xFF = 次の命令を65回実行
```

- 直後の1バイト命令を指定回数繰り返す
- FORのネストは禁止
- FRAME_CONTROL系命令への適用も許可

**エンコーダ生成禁止：**

| 値 | 実行回数 | 理由 |
|----|---------|------|
| `0xC0`（FOR 0） | 2回 | 命令を2回記述すれば等価。FOR出現が冗長 |
| `0xC1`（FOR 1） | 3回 | 命令を3回記述すれば等価。FOR出現が冗長 |

> デコーダは FOR 0・FOR 1 を処理可能とするが、エンコーダはこれらを生成してはならない。

---

### 6-3. SHIFT_BIT

```
bit7=0, bit6=1
bit5   : 符号X（0=＋方向, 1=－方向）
bit4-3 : X移動量（0–3ドット）
bit2   : 符号Y（0=＋方向, 1=－方向）
bit1-0 : Y移動量（0–3ドット）

オペコード例：
  0x41 = X+0, Y+1（下方向1ドット）
  0x54 = X+1, Y+0（右方向1ドット）
  0x6A = X-1, Y-2（左1・上2ドット）
```

- 微小差分補正専用命令
- 移動量は各軸0〜3ドット、符号付き
- 4ドット以上の移動は対象外（RLE_BLOCK等で表現）
- 隣接ブロックには影響せず、ブロック内部のみで完結

**パディングルール：**

シフト後の空きビットは **移動前ブロックの端ビット値** で埋める。

```
X方向（水平）シフト例：
  左シフト(+X)：0b10101110 → 01011100
    右端空きを「移動前ブロックの右端ビット = 0」で埋める

  右シフト(-X)：0b10101110 → 11010111
    左端空きを「移動前ブロックの左端ビット = 1」で埋める

Y方向（垂直）シフトも同様に、
  上シフト(+Y)：最下行の空きを移動前ブロックの最下行で埋める
  下シフト(-Y)：最上行の空きを移動前ブロックの最上行で埋める
```

**デコード手順：**

```
1. 前フレームの同位置ブロックを取得
2. X移動量・符号に従って水平シフトを適用
   空きビットは移動前ブロックの端ビット値で埋める
3. Y移動量・符号に従って垂直シフトを適用
   空きビットは移動前ブロックの端ビット値で埋める
4. シフト結果をカレントフレームの同位置ブロックに書き込む
```

**エンコーダ生成禁止：**

```
SHIFT_BIT(X=0, Y=0) は生成禁止 → SKIP_BLOCK を使用すること
```

---

### 6-4. BLOCK_INVERT

```
bit7=0, bit6=0, bit5=0
bit4-0 : 値0〜31 → +1オフセット → 実行回数 1〜32

オペコード例：
  0x00 = INVERT 1ブロック
  0x1F = INVERT 32ブロック
```

- 前フレームの同位置ブロックを参照し、全ビット反転して出力
- カレントブロックポインタをN個進める

---

### 6-5. RLE_BLOCK（8×8固定）

```
bit7=0, bit6=0, bit5=1, bit4=0
bit3   : 走査方向（0=横優先, 1=縦優先）
bit2-1 : 開始位置
           00 = x0y0（左上）
           01 = x7y0（右上）
           10 = x0y7（左下）
           11 = x7y7（右下）
bit0   : 開始色（0=黒基準, 1=白基準）

後続データ：3バイト固定（ランデータのビットパック）
```

**走査パターン（8パターン）：**

| パターン | bit3 | bit2-1 | 主走査 | 副走査 | 説明 |
|---------|------|--------|--------|--------|------|
| 0 | 0 | 00 | X+1 | Y+1 | 左上→右、行間下 |
| 1 | 0 | 01 | X-1 | Y+1 | 右上→左、行間下 |
| 2 | 0 | 10 | X+1 | Y-1 | 左下→右、行間上 |
| 3 | 0 | 11 | X-1 | Y-1 | 右下→左、行間上 |
| 4 | 1 | 00 | Y+1 | X+1 | 左上→下、列間右 |
| 5 | 1 | 01 | Y+1 | X-1 | 右上→下、列間左 |
| 6 | 1 | 10 | Y-1 | X+1 | 左下→上、列間右 |
| 7 | 1 | 11 | Y-1 | X-1 | 右下→上、列間左 |

**ビットパック仕様（後続3バイト）：**

```
6bit × 4 runs → 3 bytes リトルエンディアン

  Byte0 = (run0 & 0x3F) | ((run1 & 0x03) << 6)
  Byte1 = ((run1 >> 2) & 0x0F) | ((run2 & 0x0F) << 4)
  Byte2 = ((run2 >> 4) & 0x03) | (run3 << 2)
```

**デコード手順：**

```
1. オペコードから走査パターン(bit3-1)と開始色(bit0)を取得
2. 後続3バイトから4つのラン長を復元
3. 走査パターンの座標列 path[] に従ってピクセルを配置する
   ※ path[i] = (x, y) → block[y][x] に書き込む
   ※ 行方向にreshapeしてから書くのは誤り
4. 64ドットを塗り終えたらブロック完成
```

> **注意:** ランの値はbitmapへの書き込み順を path[] に従って逐次適用する。
> flatten() → reshape() で書いた場合、横走査以外のパターンで座標が狂う。

**エンコーダ最適化：**

8パターン全てを試算し、4ラン以内に収まる最小バイト数の組み合わせを選択する。
4ランを超える場合は 6-6節 RLE_BLOCK_8 または 6-8節 XOR_BLOCK を試算する。

---

### 6-6. RLE_BLOCK_8（後続6バイト）

ランが5-8個必要なブロック向けの拡張RLE命令。FRAME_CONTROL空間の空きスロットを利用する。

```
オペコード一覧（6種）:
  0x38 : 横/左上（パターン0）黒基準
  0x39 : 横/左上（パターン0）白基準
  0x3A : 横/右上（パターン1）黒基準
  0x3B : 横/右上（パターン1）白基準
  0x3D : 縦/左上（パターン4）黒基準
  0x3E : 縦/左上（パターン4）白基準

後続データ：6バイト固定（6bit × 8ラン）
```

**ビットパック仕様（後続6バイト）：**

```
前半3バイト: run0-run3（RLE_BLOCK_4と同一形式）
後半3バイト: run4-run7（同一形式を繰り返す）

  Byte0 = (run0 & 0x3F) | ((run1 & 0x03) << 6)
  Byte1 = ((run1 >> 2) & 0x0F) | ((run2 & 0x0F) << 4)
  Byte2 = ((run2 >> 4) & 0x03) | (run3 << 2)
  Byte3 = (run4 & 0x3F) | ((run5 & 0x03) << 6)
  Byte4 = ((run5 >> 2) & 0x0F) | ((run6 & 0x0F) << 4)
  Byte5 = ((run6 >> 4) & 0x03) | (run7 << 2)
```

**デコード手順：**

```
1. オペコードテーブルから走査パターンインデックスと開始色を取得
2. 後続6バイトから8つのラン長を復元
3. RLE_BLOCK_4 と同様に path[] に従いピクセルを配置
4. ラン長0は末尾パディング扱い（残りは前の色で埋める）
```

**エンコーダ採用条件：**

```
RLE_BLOCK_4（4B）が失敗（5ラン以上必要）で、
かつ XOR_BLOCK（2+NB）より小さい場合に採用。
7B < MASTER_BLOCK(9B) なので必ず MASTER より有利。
```

---

### 6-7. FILL_BLOCK

```
bit7=0, bit6=0, bit5=1, bit4=1, bit3=0
bit2   : 色（0=黒, 1=白）
bit1-0 : 繰り返し回数（00=1回, 01=2回, 10=3回, 11=4回）

オペコード例：
  0x70 = 黒1ブロック
  0x73 = 黒4ブロック
  0x74 = 白1ブロック
  0x77 = 白4ブロック
```

- 指定色でブロックを塗りつぶす
- 繰り返し回数分カレントブロックポインタを進める

**FOR命令との組み合わせによる最大ブロック数：**

```
4回（FILL_BLOCK上限） × 65回（FOR上限） = 260ブロック
```

---

### 6-8. MASTER_BLOCK（BLOCK_STREAM内での使用）

```
オペコード：0x3C（BLOCK_STREAM内）
後続データ：8バイト固定

後続8バイト：ブロック64ビットをリトルエンディアンでパックしたもの
  1バイト目 = ブロック左上8ドット（左→右）
  2バイト目 = 次の8ドット
  ...
  8バイト目 = ブロック右下8ドット
```

- あらゆるブロックを表現できるフォールバック命令
- 全ての候補（RLE_BLOCK_4/8・XOR_BLOCK）が9バイト以上になる場合に採用
- バイト順・ビット順ともにリトルエンディアン・行方向

---

### 6-9. XOR_BLOCK（後続可変長）

前フレームとの差分（XOR）をRLEエンコードした命令。SKIP_BLOCK が使えない「変化が少ない」ブロックに最適。

```
オペコード：0x3F（ブロックコンテキスト専用）
  ※ フレームコンテキストでは 0x3F = INVERT_PREV_FRAME（別命令）

後続データ：
  [1バイト: RLEデータ長 N]
  [N バイト: XORマスクのRLEデータ]

RLEデータバイト形式:
  bit7   : マスク値（0=変化なし, 1=ビット反転）
  bit6-0 : ラン長（1-63）

合計バイト数 = 2 + N
```

**復号手順：**

```
1. XORマスク（64ピクセル分）をRLEデータから復元する
   マスク値0 = 対応位置のprevビットをそのまま使用
   マスク値1 = 対応位置のprevビットを反転
2. curr_block = prev_block XOR xor_mask
   （行方向・左→右・上→下 の順）
```

**エンコーダ採用条件：**

```
XOR_BLOCK の合計バイト数 = 2 + N
N はマスクのRLEバイト数（連続する同値ビットのラン）

MASTER_BLOCK(9B) との比較:
  N <= 6 → XOR_BLOCK(2+N <= 8B) < MASTER(9B) → XOR_BLOCK を採用
  N >= 7 → MASTER を採用

典型的な効果:
  変化ピクセルが数個のブロック → N = 2〜4バイト → 4〜6バイト
  MASTER(9B) から 3〜5バイト削減
  変化が多いブロックほど N が増え MASTER に収束する
```

**XOR_BLOCK が有利な典型例：**

```
前フレーム: 00000000  （全黒ブロック）
現フレーム: 00000100  （1ピクセルだけ白）

XORマスク : 00000100
  → [0×5][1×1][0×58] = 3バイト
XOR_BLOCK : 2 + 3 = 5バイト  vs MASTER_BLOCK 9バイト
```

---

## 7. フレーム単位命令詳細

### 7-1. SKIP_FRAME（0x39）

前フレームの全ブロックをそのままカレントフレームにコピーする。後続データなし。

### 7-2. FRAME_FILL_BLACK（0x3A）

カレントフレームの全画素を0（黒）で塗りつぶす。後続データなし。

### 7-3. FRAME_FILL_WHITE（0x3B）

カレントフレームの全画素を1（白）で塗りつぶす。後続データなし。

### 7-4. INVERT_PREV_FRAME（0x3F）

前フレームの全ビットを論理反転（NOT）してカレントフレームとする。後続データなし。

```
curr_frame[x][y] = NOT( prev_frame[x][y] )

処理規則：
  - 全画素に対してビット反転のみ
  - パディング処理なし・特殊処理なし
  - 0=黒 → 1=白、1=白 → 0=黒 に単純変換
```

### 7-5. MASTER_FRAME（0x3E）

フレーム全体のrawビットデータ。後続データのサイズは固定。

```
後続データサイズ = (画像幅 × 画像高さ) ÷ 8 バイト
バイト順  ：リトルエンディアン（MASTER_BLOCKと同一）
ビット順  ：行方向（左→右、上→下）
```

> ⚠️ **BMP形式はビッグエンディアンのため：**
> - エンコード時：BMP読み込み後にバイト順変換を行ってから格納する
> - デコード時　：リトルエンディアンで読み出し、BMP書き出し時にバイト順変換を行う

### 7-6. RLE_FRAME（0x30〜0x37）

フレーム全体をRLEで表現する。オペコードのbit2-0が走査パターンを決定する。

**走査パターン（再掲）：**

```
オペコード = 0x30 | (scan_dir << 2) | (start_y << 1) | start_x
```

**RLEデータバイト構造（後続データ）：**

```
各バイト：
  bit7   : 色（0=黒, 1=白）
  bit6-0 : ラン長（1〜127）

0x00 : 終端マーカー（ただし正常終了はピクセルカウントで判定）
```

**ラン長ルール：**

- ラン長は **1以上127以下**
- 127ドット超の連続同色は複数ランに分割して表現

```
例：200ドット黒 = 0b0_1111111 + 0b0_1001001
```

**デコード手順：**

```
1. オペコードから scan_dir / start_y / start_x を抽出
2. 走査パターンに従い pixel 座標列を決定
   - scan_dir=0: 外ループ=y（start_y方向）, 内ループ=x（start_x方向）
   - scan_dir=1: 外ループ=x（start_x方向）, 内ループ=y（start_y方向）
3. 後続RLEバイトから (color, run_len) を順次読み出す
4. 座標列の順に run_len ピクセル分 color を書き込む
5. 書き込み済みピクセル数が width × height に達したら終了
```

**マイコン向け実装のポイント：**

```
中間バッファ不要。RLEストリームを読みながら gram に直接書き込む。
必要ローカル変数:
  run_color(1B) + run_remain(1B) + x(2B) + y(2B) = 6バイト
```

**エンコーダ最適化：**

```
8パターン全てのRLEバイト数を試算し最小を採用する。
コンテンツ特性との対応:
  水平コンテンツ（横縞、文字）     → 0x30 横/左上 が有利
  垂直コンテンツ（縦縞、縦スクロール）→ 0x34 縦/左上 が有利
  右→左方向コンテンツ             → 0x31 横/右上 が有利
  Bottom-up コンテンツ            → 0x32 横/左下 が有利
```

**終端検出：**

```
ピクセルカウントによる確定終了（幅×高さに達した時点）
ラン長1以上ルールにより有効RLEバイト内に0x00は出現しない
```

### 7-7. BLOCK_STREAM（0x30）

ブロック単位命令列の開始マーカー。後続にブロック命令が全ブロック分格納される。

```
デコード手順：
1. 0x30を読んだらブロック命令列モードに入る
2. カレントブロックポインタを0に初期化
3. 全ブロック数（blocks_x × blocks_y）に達するまでブロック命令を読み続ける
4. 全ブロック処理完了でBLOCK_STREAMモード終了
5. 次のFRAME_DELIMITERへ
```

---

## 8. MASTER_BLOCK（フレーム命令としての使用）

FRAME_CONTROL命令として 0x3C で呼び出した場合の仕様。

```
後続データ：8バイト × 1ブロック分
バイト順：リトルエンディアン
ビット順：行方向（左→右、上→下）
```

> ⚠️ BMP形式との変換に注意。

---

## 9. 最適化選択アルゴリズム

BadCodecエンコーダは、ブロック単位・フレーム単位それぞれで**全候補を試算し最小バイト数の表現を採用する**。

---

### 9-1. ブロック単位の最適命令選択

エンコーダは2フェーズで選択する。

**Phase-1: 1バイト命令（条件一致で即採用・後続計算なし）**

```
優先順位（高い順）:
  SKIP_BLOCK    前フレームと完全一致
  SHIFT_BIT     前フレームを±3ドットシフトして完全一致（1px移動から優先探索）
  FILL_BLOCK    全黒 or 全白
  BLOCK_INVERT  前フレームの完全反転一致

1バイト命令が1つでも見つかった時点で即返す。
Phase-2 の計算は一切行わない。
```

**Phase-2: 複数バイト候補を全試算・最小採用**

```
候補A: RLE_BLOCK_4（4B）
         8走査パターン × 2開始色 = 16通りを試算
         4ラン以内に収まる場合のみ有効

候補B: XOR_BLOCK（2+NB）
         prev XOR curr のマスクを RLE 化して N バイトを計算
         N <= 6 の場合のみ MASTER(9B) より有利

候補C: RLE_BLOCK_8（7B）
         候補Aが失敗（5ラン以上必要）の場合のみ試算
         6パターンを試算し8ラン以内に収まる場合のみ有効

候補D: MASTER_BLOCK（9B）
         無条件フォールバック

→ 候補A〜Dの中で最小バイト数を採用
→ 同サイズの場合 A > B > C > D の順で優先
```

**ブロック選択フロー（rev.15）：**

```
[カレントブロック評価開始]
       │
       ▼
[Phase-1: 1バイト命令判定]
   ├─ prev == curr           → SKIP_BLOCK（1B）即採用
   ├─ SHIFT(1px) == curr     → SHIFT_BIT（1B）即採用
   ├─ SHIFT(2px) == curr     → SHIFT_BIT（1B）即採用
   ├─ SHIFT(3px) == curr     → SHIFT_BIT（1B）即採用
   ├─ 全黒 or 全白           → FILL_BLOCK（1B）即採用
   ├─ prev反転 == curr       → BLOCK_INVERT（1B）即採用
   └─ 全て不一致
           │
           ▼
   [Phase-2: 複数バイト候補を全試算]
           │
   [RLE_BLOCK_4 試算] → 4B or 失敗
   [XOR_BLOCK   試算] → 2+NB（N<=6なら有効）
   [RLE_BLOCK_8 試算] → 7B（RLE4失敗時のみ）
   [MASTER_BLOCK]    → 9B（常に有効）
           │
           ▼
   [最小バイト数を採用]
```

**サイズ比較表：**

```
命令             バイト数  条件
SKIP_BLOCK         1B    prev完全一致
FILL_BLOCK         1B    全黒/全白
BLOCK_INVERT       1B    prev完全反転
SHIFT_BIT          1B    ±3px移動一致
RLE_BLOCK_4        4B    4ラン以内
XOR_BLOCK          2+NB  N<=6（典型2-5B）
RLE_BLOCK_8        7B    8ラン以内
MASTER_BLOCK       9B    フォールバック
```

---

### 9-2. FOR命令による連続ブロック圧縮試算

同一命令が連続するブロックに対して `FOR(n) + CMD(i)` の組み合わせを全探索し最小バイト数を採用する。

**アルゴリズム（rev.13）：**

```
N ブロックのランに対して N = n × i + j と分解する。

  n : FOR の繰り返し回数 (2〜65)
  i : CMD 1回あたりのブロック数 (1〜max_inner)
  j : 余り (0〜N-1)

探索手順:
  全 i (1..max_inner) について n = N // i の前後 (n, n+1) を候補とする。
  各候補の総バイト数 = 2(FOR+CMD) + naive_bytes(余りj) を算出。
  最小バイト数の (n, i, j) を採用する。
  余り j は再帰的に同アルゴリズムを適用する。
  FOR禁止ルール (n < 2) を厳守。

max_inner の命令別上限:
  SKIP_BLOCK   : 64 (0x80〜0xBF)
  BLOCK_INVERT : 32 (0x00〜0x1F)
  FILL_BLOCK   : 4  (0x30〜0x37)
```

**効果例（128×64画像 / 全ブロック数128）：**

```
N ブロックのSKIPラン:

      N    旧バイト  新バイト  エンコード内容
      64      1       1      BF           (SKIP×64)
     128      2       2      BF BF        (SKIP×64 + SKIP×64)
     255      4       2      F1 84        FOR(51)+SKIP(5)
     256      4       2      FE 83        FOR(64)+SKIP(4)
     512      8       2      FE 87        FOR(64)+SKIP(8)
    1024     16       2      FE 8F        FOR(64)+SKIP(16)
    4160     65       2      FF BF        FOR(65)+SKIP(64) ← 最大

FILL_BLACK N=260 ブロック:
  旧: 65バイト (FILL×4 を65回)
  新:  2バイト  FOR(65)+FILL(4) = FOR(65)×FILL_4

128×64 全ブロック FILL = FOR(32)+FILL(4) = 2バイト
```

**素因数的解釈：**

```
N が max_inner の倍数になる (n,i) が見つかれば余りゼロ → 2バイト確定。
N が素数の場合は余りが必ず発生するが、2〜3バイトで収まる。
最悪ケースでも旧アルゴリズム (⌈N/max_inner⌉ バイト) 以下を保証。
```

---

### 9-3. フレーム単位の最適表現選択

全ブロックの最適命令が確定した後、フレーム全体の表現を試算し最小バイト数を採用する。

```
候補A: BLOCK_STREAM（0x3C）合計バイト数
         9-1・9-2で確定した各ブロック命令の総和 + 1（0x3Cマーカー）

候補B: RLE_FRAME（0x30〜0x37）バイト数
         8走査パターン全試算・最小バイト数のオペコードを採用
         1 + RLEデータバイト数

候補C: MASTER_FRAME（0x3E）バイト数（固定）
         1 + (画像幅 × 画像高さ) ÷ 8 バイト

候補D: フレーム単一命令（1バイト・条件付き・最優先）
         SKIP_FRAME（0x39）     前フレームと完全一致の場合のみ
         FRAME_FILL_BLACK（0x3A）全画素0の場合のみ
         FRAME_FILL_WHITE（0x3B）全画素1の場合のみ
         INVERT_PREV_FRAME（0x3F）前フレームの完全反転の場合のみ

→ 候補Dが成立すれば即採用（1バイト確定・他候補の計算不要）
→ 成立しない場合は候補A・B・Cを全て算出し最小を採用
```

**天秤の原理：**

```
BLOCK_STREAM が有利なケース:
  差分が少ない (SKIP/SHIFTが多い)
  ブロック単位での局所的な変化が主
  静止・微動フレーム

RLE_FRAME が有利なケース:
  フレーム全体が大きく変化する
  水平・垂直方向に連続した領域が多い
  コンテンツの走査方向と RLE走査方向が一致する

MASTER_FRAME が有利なケース:
  ランダムノイズ的なフレーム
  上記いずれも MASTER_FRAME のサイズ(w×h/8+1)を超える場合

この3候補の数値比較により、
フレーム特性に応じた最適表現が自動的に選択される。
```

**フレーム選択フロー（rev.13）：**

```
[フレーム評価開始]
       │
       ▼
[候補D: フレーム単一命令判定]
   ├─ 前フレームと完全一致    → SKIP_FRAME（1B）即採用
   ├─ 全画素黒               → FRAME_FILL_BLACK（1B）即採用
   ├─ 全画素白               → FRAME_FILL_WHITE（1B）即採用
   ├─ 前フレームの完全反転    → INVERT_PREV_FRAME（1B）即採用
   └─ 該当なし
           │
           ▼
   [候補A] ブロック単位エンコード（9-1・9-2適用）
           BLOCK_STREAM(1B) + 全ブロック命令列
           │
   [候補B] RLE_FRAME 8走査パターン全試算
           最小バイト数のオペコード(0x30-0x37)を選択
           │
   [候補C] MASTER_FRAME（固定長）
           1 + w×h/8 バイト
           │
           ▼
   [候補A・B・Cのバイト数を比較 → 最小を採用]
```

---

### 9-4. 最適化優先順位まとめ

```
優先度1（1バイト確定・即採用）:
  候補D: SKIP_FRAME / FRAME_FILL_BLACK / FRAME_FILL_WHITE / INVERT_PREV_FRAME
  条件を満たす場合は他候補の計算を行わずに即採用。

優先度2（数値比較で決定）:
  候補A: BLOCK_STREAM + FOR最適化済みブロック命令列
    → 差分少・局所変化フレームに有利
  候補B: RLE_FRAME（8走査パターン最小選択）
    → 大域変化・走査方向一致フレームに有利
  候補C: MASTER_FRAME（固定長フォールバック）
    → ランダムノイズ的フレームのみ採用される

最終決定は常にバイト数の数値比較による。
```

---

## 10. エンコーダ生成禁止ルール一覧

| 命令 | 禁止内容 | 代替 |
|------|---------|------|
| FOR 0（0xC0） | 2回繰り返しはFOR不要 | 命令を2回記述 |
| FOR 1（0xC1） | 3回繰り返しはFOR不要 | 命令を3回記述 |
| SHIFT_BIT(0,0) | SKIP_BLOCKと等価 | SKIP_BLOCK を使用 |

---

## 11. 画像分割ブロック

```
blocks_x    = image_width  / 8
blocks_y    = image_height / 8
block_index = (y / 8) * blocks_x + (x / 8)
```

- 0始まり、左上を起点、X方向優先、行末でY方向へ進む
- 画像サイズは8の倍数でなければならない
- 余りが出る場合は処理不可

---

## 12. CLI 仕様（エンコード・デコード共通）

### 12-1. CLI 引数一覧

| オプション | 必須 | デフォルト | エンコード | デコード | 説明 |
|-----------|------|-----------|-----------|---------|------|
| `-t` / `--task` | **はい** | なし | `e` | `d` | 処理モード指定 |
| `-p` / `--path` | **はい** | なし | ✅ | ✅ | BMPファイル格納・出力ディレクトリ |
| `-n` / `--suffix` | いいえ | `frame_` | ✅ | ✅ | ファイル名接頭辞 |
| `-s` / `--start` | いいえ | `0001` | ✅ | ✅ | 開始フレーム番号（4桁ゼロ埋め）|
| `-e` / `--end` | エンコード時必須 | なし | ✅ | ✅ | 終了フレーム番号 |
| `-o` / `--output` | いいえ | `output.bad` | ✅ | － | 出力.badファイル名 |
| `-i` / `--input` | デコード時必須 | なし | － | ✅ | 入力.badファイル名 |

### 12-2. ファイル解決規則

**エンコード時（入力BMPの解決）：**

```
<path>/<suffix><frame_number:04d>.bmp

例：-p ./frames  -n frame_  -s 1  -e 6572
→ ./frames/frame_0001.bmp 〜 ./frames/frame_6572.bmp
```

**デコード時（出力BMPの解決）：**

```
<path>/<suffix><frame_number:04d>.bmp

例：-t d  -i output.bad  -p ./out  -n frame_  -s 1
→ ./out/frame_0001.bmp, ./out/frame_0002.bmp, ...

出力先ディレクトリが存在しない場合は自動生成する
```

### 12-3. デコード時のフレーム番号解決

```
-s で指定した番号を起点に、デコードしたフレームを
連番で出力する。

例：-s 0001 の場合
  1フレーム目 → <path>/<suffix>0001.bmp
  2フレーム目 → <path>/<suffix>0002.bmp
  ...

-e を省略した場合は .bad ファイルのヘッダーに記録された
総フレーム数をもとに全フレームをデコードする。
-e を指定した場合は指定フレーム数でデコードを終了する。
```

---

## 13. マルチCPU対応仕様

### 13-1. 前提条件

```
-p で指定するディレクトリには連番BMPファイルが格納されている。
-s / -e で指定した範囲のフレームが全て存在することを前提とする。

例：-s 1 -e 6572 の場合
  frame_0001.bmp 〜 frame_6572.bmp が全て存在する
```

この前提により、**フレームインデックスを事前に確定できる**ため、
1フレーム = 1コアという単純な分担が可能になる。

---

### 13-2. 対応範囲

| 処理 | マルチCPU対応 | 方式 |
|------|-------------|------|
| エンコード | ✅ | 1フレーム1コアの静的分担 |
| デコード | ✅ | 1フレーム1コアの静的分担 |

---

### 13-3. フレーム分担方式

```
総フレーム数 = end - start + 1
並列数       = cpu_count()（論理CPU数を自動取得）

各コアへのフレーム割り当て：
  コア0 → フレーム start, start+N, start+2N, ...
  コア1 → フレーム start+1, start+1+N, ...
  コア2 → フレーム start+2, start+2+N, ...
  ...
  （N = 並列数）

または imap_unordered による動的ディスパッチも可。
いずれの方式でも以下の書き出しレギュレーションに従う。
```

---

### 13-4. 書き出しレギュレーション

各コアは処理完了後、以下のルールに従って出力を行う。

```
[コアが1フレームの処理を完了]
        │
        ▼
[自分が処理したフレーム番号 == 現在の書き出し待ちフレーム番号？]
        │
   ┌────┴────┐
   YES       NO
   │         │
   ▼         ▼
[即時書き出し]  [メモリバッファに保持]
   │
   ▼
[書き出し待ちポインタをインクリメント]
        │
        ▼
[バッファに次番号のフレームが存在する？]
        │
   ┌────┴────┐
   YES       NO
   │         │
   ▼         ▼
[連続書き出し]  [待機・次コア完了を待つ]
```

**具体例（4コア、フレーム1〜8）：**

```
コア0がフレーム3を完了 → 書き出し待ちは1 → メモリ保持
コア1がフレーム1を完了 → 書き出し待ちは1 → 即時書き出し → 待ちは2へ
コア2がフレーム2を完了 → 書き出し待ちは2 → 即時書き出し → 待ちは3へ
                                         → バッファに3あり → 連続書き出し → 待ちは4へ
```

この方式により：
- **ファイルへの書き出し順序が保証される**
- **メモリ使用量が最小化される**（先行完了フレームのみバッファ）
- **ロック機構が不要**（書き出しポインタは単一スレッドが管理）

---

### 13-5. エンコード時の自己検証（Self-Verify）

各コアはエンコード完了後、自身がエンコードしたデータを即座にデコードし、
元のBMPデータと一致するかを検証する。

```
処理フロー（1フレーム分、コア内で完結）：

[BMPフレームを読み込む] → original[]
        │
        ▼
[エンコード実行] → encoded_bytes[]
        │
        ▼
[encoded_bytes[] をデコード] → decoded[]
        │
        ▼
[original[] == decoded[] ？]
        │
   ┌────┴────┐
   YES       NO
   │         │
   ▼         ▼
[検証OK]   [検証失敗]
[書き出し]  [エラーログ出力]
            [MASTERフレームにフォールバックして再エンコード]
            [再検証]
```

**検証失敗時のフォールバック：**

```
1. 問題フレームを MASTER_FRAME で再エンコード
   （MASTER_FRAMEは全画素をrawで格納するため劣化なし）
2. 再度デコード検証を実施
3. 再検証も失敗した場合はエラーとして処理を中断し
   フレーム番号・エンコード命令をログに出力する
```

**検証のコスト：**

```
検証はコア内で完結するため並列処理の効率に影響しない。
デコード処理は軽量（ビット演算のみ）のため
エンコードコストに対して無視できる水準。
```

---

### 13-6. デコード時のマルチCPU

```
使用ライブラリ：multiprocessing.Pool
並列単位      ：1フレーム = 1コア
書き出しルール：13-4と同様のレギュレーションに従う

注意事項：
  デコードはフレーム間差分（前フレーム参照命令）があるため、
  各コアには前フレームのデコード結果を引数として渡す。

  前フレーム参照命令：
    SKIP_FRAME / SKIP_BLOCK / BLOCK_INVERT /
    SHIFT_BIT / INVERT_PREV_FRAME

  前フレームが未完了の場合は完了を待機してから処理を開始する。
  FRAME_FILL_BLACK / FRAME_FILL_WHITE は前フレーム不要のため
  即時並列実行可能。
```

---

## 14. エンコーダ UI 仕様

### 14-1. フレーム読み込み UI

**表示タイミング：** 50フレームごと・最終フレーム読み込み時

```
Loading Frames: [####################..............] 50% (3286/6572)
```

| 項目 | 内容 |
|------|------|
| プログレスバー幅 | 40文字固定 |
| 完了部 | `#` |
| 未完了部 | `.` |
| 割合表示 | 整数 % |
| カウンタ | (current / total) |

### 14-2. エンコード開始メッセージ

```
Starting Encode (Shift & FOR optimized)...
```

### 14-3. エンコード中アナライズ UI

#### ブロック種別カラー定義

| 記号 | 命令種別 | ANSIカラー | カラーコード |
|------|---------|-----------|------------|
| `S` | SKIP_BLOCK | 緑 | `\033[92m` |
| `F` | FILL_BLOCK | 白 | `\033[97m` |
| `R` | RLE_BLOCK | 黄 | `\033[33m` |
| `X` | SHIFT_BIT | シアン | `\033[36m` |
| `M` | MASTER_BLOCK | 赤 | `\033[91m` |
| `I` | BLOCK_INVERT / フレーム反転系 | マゼンタ | `\033[35m` |

#### 画面構成

エンコード開始時に画面クリア（`\033[2J\033[H`）後、**ANSI上書き更新**（`\033[H`）でリアルタイム表示。

```
BadCodec v4.5.0 Encoder
████████████░░░░░░░░ 60.0%
Frame: 0042 | Size:   384 | Avg: 12.34%
S S R R M S S F F R S S R R R S S S F F S S R R S S R R R S
I R R S S S F F R R S S R R S S R S F F S S M R R R S S S R
...（最大8行 × 最大30列）
```

#### 各要素の仕様

**プログレスバー：**

| 項目 | 内容 |
|------|------|
| バー幅 | 20文字固定 |
| 完了部 | `█` |
| 未完了部 | `░` |
| 割合表示 | 小数点1桁 % |
| 算出式 | `curr_frame / total_frames` |

**フレーム情報行：**

| 項目 | 書式 | 内容 |
|------|------|------|
| Frame | `%04d` | カレントフレーム番号 |
| Size | `%5d` | エンコード後バイト数 |
| Avg | `%5.2f%%` | 直近90フレーム移動平均圧縮率 |

**圧縮率算出式：**

```
圧縮率(%) = (エンコード後バイト数 / 生データバイト数) × 100
生データバイト数 = (画像幅 × 画像高さ) ÷ 8
移動平均ウィンドウ = 直近90フレーム（リングバッファ）
```

**ビジュアルブロックマップ：**

| 項目 | 内容 |
|------|------|
| 表示行数 | 最大8行（`min(8, 画像高さ÷8)`）|
| 表示列数 | 最大30列（`min(ブロック列数, 30)`）|
| 各セル | 記号1文字 + スペース1文字、ANSIカラー付き |
| 更新 | フレームごとに上書き更新 |

**フレーム単一命令時のマップ表示：**

| フレーム命令 | 記号 | カラー |
|------------|------|--------|
| SKIP_FRAME | `S` | 緑 |
| FRAME_FILL_BLACK | `F` | 白 |
| FRAME_FILL_WHITE | `F` | 白 |
| INVERT_PREV_FRAME | `I` | マゼンタ |

#### 表示更新フロー

```
[フレームエンコード完了]
  → カーソルを画面先頭へ（\033[H）
  → タイトル行出力
  → プログレスバー更新
  → フレーム情報行更新（Frame/Size/Avg）
  → ビジュアルブロックマップ描画（上部8行×左端30列）
  → 次フレームへ
```

### 14-4. エンコード完了メッセージ

```
Done. Saved to output.bad (123456 bytes)
```

---

## 15. デコーダ UI 仕様

### 15-1. デコード開始メッセージ

```
BadCodec v4.5.0 Decoder
Input : output.bad
Output: ./out/frame_%04d.bmp
Frames: 6572
Starting Decode...
```

| 項目 | 内容 |
|------|------|
| Input | `-i` で指定した入力ファイル名 |
| Output | `-p`/`-n` から解決した出力パスパターン |
| Frames | ヘッダーから読み取った総フレーム数 |

### 15-2. デコード中 UI

**表示方式：** ANSI上書き更新（`\033[H`）によるリアルタイム表示

**画面構成：**

```
BadCodec v4.5.0 Decoder
████████████░░░░░░░░ 60.0%
Frame: 0042 / 6572 | Written: ./out/frame_0042.bmp
```

**各要素の仕様：**

| 要素 | 書式 | 内容 |
|------|------|------|
| プログレスバー幅 | 20文字固定 | `█`完了 / `░`未完了 |
| 割合表示 | 小数点1桁 % | `curr / total` |
| Frame | `%04d / %d` | カレント / 総フレーム数 |
| Written | パス文字列 | 直近書き込み完了ファイル名 |

### 15-3. デコード完了メッセージ

```
Decode Complete. (6572 frames → ./out/)
```

---

## 16. 拡張命令仕様（将来予約）

### 16-1. DELTA_FRAME の位置づけ変更

DELTA_FRAMEは通常命令空間から**拡張命令空間へ移動**する。

**理由：** 詳細仕様が未確定であり、通常命令空間に確定前の命令を配置することでデコーダの挙動が不定になるリスクがあるため。

### 16-2. 拡張命令のオペコード体系

通常フレーム命令空間（0x38〜0x3F）とは独立した拡張プレフィックスバイトを導入する。

```
拡張命令プレフィックス：0xFF
0xFF の直後に続く1バイトで拡張命令種別を識別する

例：
  0xFF 0x01 = DELTA_FRAME（将来定義）
  0xFF 0x02 = 予約
  0xFF 0x03 = 予約
  ...
  0xFF 0xFF = 予約
```

### 16-3. 拡張命令の処理規則

```
デコーダの処理：
  0xFF を読んだ場合、次の1バイトを読み拡張命令として解釈する
  未定義の拡張命令を受け取った場合はスキップし次の命令へ進む
  （フォールトトレランス：未知の拡張命令で停止しない）

エンコーダの処理：
  拡張命令が未定義の間はエンコーダから生成しない
  DELTA_FRAME相当の処理が必要な場合は代替命令で表現する
```

### 16-4. DELTA_FRAME（拡張命令・未定義）

| 項目 | 状態 |
|------|------|
| オペコード | `0xFF 0x01`（予約）|
| 詳細仕様 | 完全未定義 |
| 実装状態 | スタブのみ実装可 |

---

## 17. 未定義事項（実装依存）

| 項目 | 状態 |
|------|------|
| DELTA_FRAME 詳細仕様 | 拡張命令へ移動・完全未定義 |

> 現時点で実装に影響する未定義事項はありません。

---

## 18. 変更履歴

| rev | 日付 | 内容 |
|-----|------|------|
| 1 | 2026-03-10 | 初版。SHIFT_BIT構造、RLE_BLOCK分木復元、FILL_BLOCK確定 |
| 2 | 2026-03-10 | SKIP_BLOCK 1–64確定。FOR 0/1禁止。FOR+FRAME_CONTROL許可。SHIFTパディング確定。16×16RLE削除。MASTER_BLOCKリトルエンディアン確定。RLE_FRAME 1bit+7bit確定。0x00終端検出確定 |
| 3 | 2026-03-10 | CLI引数・Loading UI・エンコード開始メッセージ・ペンディング事項追記 |
| 4 | 2026-03-10 | ブロック・フレーム最適化アルゴリズム追記。完全版統合 |
| 5 | 2026-03-10 | ソースコードよりアナライズUI仕様抽出・追記 |
| 6 | 2026-03-10 | ヘッダー完全構造（MD5・総フレーム数・ブロックサイズ）追記。0x30 BLOCK_STREAMマーカー定義。フレーム先頭オペコード一覧整備。SHIFT_BITデコード手順追記。オペコード早見表追記。ソースコードなしで実装可能な完全版として再構成 |
| 7 | 2026-03-10 | デコードCLI引数（-n/-s/-e対応）追記。デコードUI仕様定義。マルチCPU対応仕様追記。DELTA_FRAMEを拡張命令空間へ移動 |
| 8 | 2026-03-10 | INVERT_PREV_FRAMEを確定。単純論理反転（NOT）・パディングなし |
| 9 | 2026-03-10 | マルチCPU仕様全面改訂。書き出しレギュレーション・Self-Verify仕様追加 |
| 10 | 2026-03-10 | MASTER_FRAMEのビット順確定。リトルエンディアン・行方向（MASTER_BLOCKと同一）。エンコード・デコード時のBMPバイト順変換を明記。未定義事項がDELTA_FRAMEのみとなり実装着手可能状態に到達 |
| 11 | 2026-03-10 | ヘッダーチェックサムをMD5（16バイト）からFletcher-16（2バイト）に変更。ヘッダー全体サイズ33→19バイトに削減。C言語実装例追記。バージョン番号500→510。マイコンデコーダの最低解を達成 |
| 12 | 2026-03-15 | フレームオペコード再割り当て。0x30=RLE_FRAME（旧BLOCK_STREAM）、0x3C=BLOCK_STREAM（旧MASTER_BLOCK）、0x3D解放。フレーム命令を0x38-0x3F範囲に集約。プロトコルバージョン510→511。破壊的変更・既存.badファイル再エンコード必要 |
| 13 | 2026-03-15 | RLE_FRAMEを8走査パターンに拡張。0x30-0x37をRLE_FRAME×8パターンとして定義。bit2=走査方向、bit1=start_y、bit0=start_x。0x30（横/TL）は旧と同一動作で後方互換。エンコーダが全8パターンを試算し最小採用。プロトコルバージョン511→512 |
| 14 | 2026-03-15 | FOR最適化アルゴリズムをN=n×i+j全探索型に改訂。素因数的探索で余りゼロを優先。SPEC.md 9-2節・9-3節・9-4節を現行実装に合わせて全面更新。天秤原理（BLOCK_STREAM vs RLE_FRAME vs MASTER_FRAME）を明文化 |
| 15 | 2026-03-15 | RLE_BLOCK_8（0x38-0x3B,0x3D-0x3E, 後続6B, 8ラン対応）追加。XOR_BLOCK（0x3F, 後続可変長, prev XOR差分RLE）追加。RLE_BLOCK_4デコーダのpath[]対応バグ修正（flatten/reshape誤りを修正）。プロトコルバージョン512→513 |
